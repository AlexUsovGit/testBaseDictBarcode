<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <title>Справочник</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!--===============================================================================================-->
    <link rel="icon" type="image/png" href="../images/icons/favicon.ico"/>

</head>
<body>
 <p><a href="/main">Главная</a></p>

<div align="center" style="margin: 30px;">
    <form method="post">

        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
        <input type="text" name="label" placeholder="Наименование">
        <button type="submit">Добавить</button>
    </form>
</div>


<div style="text-align:center; ">
    <table align="center" border="1" cellpadding="0" cellspacing="0" style="width:100%;     border-color: lightgray;">
        <tbody>
        <tr style="text-align:center; font-size:16px">

            <td style="width: 6%; ">Id</td>
            <td style="width: 25%; ">Наименование</td>
            <td style="width: 6%; ">Автор</td>
            <td style="width: 6%; ">Статус</td>
            <td style="width: 25%; ">Дата обновления</td>
            <td style="width: 6%; display: none">Действие</td>
            <td style="width: 6%; ">Действие</td>

        </tr>
        {{#productNames}}
            <div>


                <tr style="text-align:left; font-size:18px">
                    <td>{{id}}</td>
                    <td>{{label}}</td>
                    <td>{{author}}</td>
                    <td>{{isActive}}</td>
                    <td>{{lastUpdate}}</td>
                    <td style="display: none">
                        <form method="post" action="">
                            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
                            <input type="text" name="filter" value="{{id}}" style="display: none">
                            <button type="submit">Редактировать</button>
                        </form>
                    </td>
                    <td>
                        <form method="post" action="deletecomposition">
                            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
                            <input type="text" name="filter" value="{{id}}" style="display: none">
                            <button type="submit">Удалить</button>
                        </form>


                    </td>

                </tr>
            </div>
        {{/productNames}}
        </tbody>
    </table>

</div>


</body>
</html>